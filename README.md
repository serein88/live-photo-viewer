# Live 图查看器

## 运行方式
- 这是纯前端静态项目。
- 使用任意静态服务器打开项目根目录并访问 `index.html`。

## 目录
- `index.html`: 页面入口
- `src/css/app.css`: 业务样式
- `src/js/app.js`: 业务逻辑
- `vendor/`: 第三方库（Viewer.js）
- `docs/`: 项目管理文档
- `agents.md`: 开发交接日志

## 协作约定
- 功能开发先更新文档，再改代码。
- 每次需求完成后更新 `agents.md`。
- 大改动优先拆分为多次小提交。
